language: python
python:
  - 3.5
  - 3.6
install:
  - pip install -r base_requirements.txt
  - pip install -U pytest pytest-cov coveralls flake8
script:
  - python -m pytest --cov .
  - flake8
after_success:
  - coveralls
